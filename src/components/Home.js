import './styles/Home.scss';

const Home = () => (
  <p className="primary-p">
    Welcome to this little website where you can search for pokemon (only Gen 1 & 2)
    and see the details for each pokemon by clicking on the corresponding button
  </p>
);

export default Home;
